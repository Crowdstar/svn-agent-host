language: php

php:
  - 7.2
  - 7.1
  - 7.0

sudo: false

install:
  - composer install -n

script:
  - ./vendor/bin/phplint
  - ./vendor/bin/phpcs -v --standard=PSR2 src tests
  - ./vendor/bin/phpunit
